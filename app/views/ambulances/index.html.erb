<meta name="format-detection" content="telephone=no">
<div class="container series-head">
  <h1>CFD C-series</h1>
  <p>The C-series was created to inventory squad apparatus. The original squads were dissolved in the late 1960s and the series was not used again until 1979. Ambulances, which had previously fallen under the catch-all G-series, were transferred to the C-series and have remained ever since. One additional note: at some point in the early '80s, ambulance numbering jumped from the C-100s to C-700s.</p>
</div>
<div class="container">
  <div class="table-responsive">
    <table class="table table-stripe">
      <thead class="thead-inverse ">
        <tr class="row-head">
          <th scope="col">Shop &#35</th>
          <th scope="col">Year</th>
          <th scope="col">Make&#47Model</th>
          <th scope="col">Original Assignment</th>
          <th scope="col">Second Assignment</th>
          <th scope="col">Third Assignment</th>
          <th scope="col">Notes</th>
          <th scope="col">Image Links</th>
        </tr>
      </thead>
      <tbody>
        <% @ambulances.each do |ambulance| %>
          <% next if ambulance.id > 104 && ambulance.id < 701 %>
          <tr>
            <th scope="row"><a href="/c-series/<%= ambulance.id %>"><%= ambulance.shop_num %></a></th>
            <td><%= ambulance.year %></td>
            <td><%= ambulance.make_model %></td>
            <td><%= ambulance.original_assign %></td>
            <td><%= ambulance.second_assign %></td>
            <td><%= ambulance.third_assign %></td>
            <td><%= ambulance.notes %></td></a>
            <td><a href=<%= ambulance.img_link %> target="_blank"><%= ambulance.caption %></a>
              <a href=<%= ambulance.img_link_two %> target="_blank"><%= ambulance.caption_two %></a>
              <a href=<%= ambulance.img_link_three %> target="_blank"><%= ambulance.caption_three %></a>
              <a href=<%= ambulance.img_link_four %> target="_blank"><%= ambulance.caption_four %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>