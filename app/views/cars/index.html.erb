<meta name="format-detection" content="telephone=no">
<div class="container series-head">
  <h1>CFD A-series</h1>
  <p>The A-series was originally for large sedans used by high-ranking CFD personnel. In more recent times, the A-series is used for any non-SUV car.</p>
</div>

<div class="container">
  <div class="table-responsive">
    <table class="table table-stripe">
      <thead class="thead-inverse ">
        <tr class="row-head">
          <th scope="col">Shop &#35</th>
          <th scope="col">Year</th>
          <th scope="col">Make&#47Model</th>
          <th scope="col">Original Assignment</th>
          <th scope="col">Second Assignment</th>
          <th scope="col">Third Assignment</th>
          <th scope="col">Notes</th>
          <th scope="col">Image Links</th>
        </tr>
      </thead>
      <tbody>
        <% @cars.each do |car| %>
          <tr>
            <th scope="row"><a href="/a-series/<%= car.id %>"><%= car.shop_num %></a></th>
            <td><%= car.year %></td>
            <td><%= car.make_model %></td>
            <td><%= car.original_assign %></td>
            <td><%= car.second_assign %></td>
            <td><%= car.third_assign %></td>
            <td><%= car.notes %></td>
            <td><a href=<%= car.img_link %> target="_blank"><%= car.caption %></a>
              <a href=<%= car.img_link_two %> target="_blank"><%= car.caption_two %></a>
              <a href=<%= car.img_link_three %> target="_blank"><%= car.caption_three %></a>
              <a href=<%= car.img_link_four %> target="_blank"><%= car.caption_four %></a>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>